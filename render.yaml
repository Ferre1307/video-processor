services:
  - type: web
    name: video-processor
    env: node
    buildCommand: |
      apt-get update && apt-get install -y ffmpeg && npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 3000
